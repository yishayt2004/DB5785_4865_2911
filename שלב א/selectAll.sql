-- הצגת כל הנתונים מטבלת Department
SELECT * FROM Department;

-- הצגת כל הנתונים מטבלת Employee
SELECT * FROM Employee;

-- הצגת כל הנתונים מטבלת Payroll
SELECT * FROM Payroll;

-- הצגת כל הנתונים מטבלת EmployeeEvaluation
SELECT * FROM EmployeeEvaluation;

-- הצגת כל הנתונים מטבלת ShiftSchedule
SELECT * FROM ShiftSchedule;

-- הצגת כל הנתונים מטבלת EmployeeTraining
SELECT * FROM EmployeeTraining;
